import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Design themes and inspiration', description: 'Browse interior design themes with room-by-room inspiration for homes in Delhi NCR.' }

export default function ThemesLayout({ children }: { children: React.ReactNode }) {
  return children
}
